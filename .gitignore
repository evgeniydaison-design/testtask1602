# Backend — не пушим, только backend-src (по README делают composer create-project + copy)
backend/

# Frontend
frontend/node_modules
frontend/.nuxt
frontend/.output
frontend/dist
frontend/.data
frontend/.cache
frontend/.env
frontend/.env.*

# Backups / temp
backend-old

# OS
.DS_Store
Thumbs.db
